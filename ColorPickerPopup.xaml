<local:BaseWindow x:Class="ArkServerManager.ColorPickerPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArkServerManager"
        xmlns:acp="clr-namespace:AdvancedColorPickerLib;assembly=AdvancedColorPickerLib"
        mc:Ignorable="d"
        Title="Select Color"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource CustomWindowStyle}"
        Width="500"
        Height="600">

    <Grid Margin="5" Background="{DynamicResource BackgroundBrush1}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Picker -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>
        <acp:AdvancedColorPicker Grid.Row="0" x:Name="PopupColorPicker" SelectedColorChanged="PopupColorPicker_SelectedColorChanged" Margin="10,5,10,5"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="5" Click="OkButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" Margin="5" IsCancel="True" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</local:BaseWindow>